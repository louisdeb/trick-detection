/*---------------------------------------------------------------------------*/

enum Roll
{
  kick,
  heel,
  no_roll
};

typedef enum Roll Roll;

/*---------------------------------------------------------------------------*/

enum Pop
{
  tail,
  nose,
  no_pop
};

typedef enum Pop Pop;

/*---------------------------------------------------------------------------*/

enum Spin
{
  frontside,
  backside,
  no_spin
};

typedef enum Spin Spin;

/*---------------------------------------------------------------------------*/
enum Trick
{
  ollie,
  nollie,
  kickflip,
  heelflip,
  no_trick
};
