#include "../utils.h"

/*---------------------------------------------------------------------------*/

void process_packet(comms_packet);
void add_reading(int, mpu_values);
void print_reading(mpu_values);